window.__BinaryWorldSourceParts=window.__BinaryWorldSourceParts||[];window.__BinaryWorldSourceParts[6]="(function(){\n'use strict';\n\nvar bwAuto={\n    active:false,\n    bs:[],\n    activeIndex:-1,\n    busy:false,\n    plan:null,\n    planner:null,\n    planToken:0\n};\n\nvar bwAutoSuppressClick=false;\n\nfunction isSameBlock(a,b){\n    return !!a&&!!b&&Number(a.layer)===Number(b.layer)&&Number(a.x)===Number(b.x)&&Number(a.y)===Number(b.y);\n}\n\nfunction bwAutoStep(x,y,side){var d=delta[side];if(!d)return null;return{x:x+d[0],y:y+d[1]};}\nfunction bwAutoRotation(side){if(side==='right')return 0;if(side==='bottom')return 90;if(side==='left')return 180;return 270;}\nfunction bwAutoGridKey(x,y,layer){return String(layer)+':'+String(x)+':'+String(y)}\nfunction bwAutoPack(x,y,layer,w,h){return Number(layer)*w*h+Number(y)*w+Number(x)}\n\nfunction bwAutoCellElement(x,y){if(!gridElement)return null;return gridElement.querySelector(\".cell[data-x='\"+x+\"'][data-y='\"+y+\"']\");}\n\nfunction bwAutoIsPortElement(el){\n    if(!el||!el.classList)return false;\n    return el.classList.contains('inputPort')||el.classList.contains('outputPort')||el.classList.contains('inputOutputPort')||el.classList.contains('nodePort')||el.classList.contains('passerOutput');\n}\n\nfunction bwAutoPortSide(el,cell){\n    if(!el||!cell)return null;\n    var c=String(el.className||''),named=[\n        ['top','(?:^|\\\\s)(?:top|inputTop|comparatorInputTop|latchInputTop)(?:\\\\s|$)'],\n        ['right','(?:^|\\\\s)(?:right|outputRight|comparatorOutput|latchOutput|passerOutput)(?:\\\\s|$)'],\n        ['bottom','(?:^|\\\\s)(?:bottom)(?:\\\\s|$)'],\n        ['left','(?:^|\\\\s)(?:left|inputLeft|comparatorInputLeft)(?:\\\\s|$)']\n    ];\n    for(var i=0;i<named.length;i++)if(new RegExp(named[i][1]).test(c))return named[i][0];\n    var r=cell.getBoundingClientRect(),p=el.getBoundingClientRect(),cx=(p.left+p.right)/2,cy=(p.top+p.bottom)/2;\n    var ds={top:Math.abs(cy-r.top),right:Math.abs(cx-r.right),bottom:Math.abs(cy-r.bottom),left:Math.abs(cx-r.left)},best='top',v=ds.top;\n    ['right','bottom','left'].forEach(function(s){if(ds[s]<v){v=ds[s];best=s}});\n    return best;\n}\n\nfunction bwAutoDomPorts(cell){\n    var out=[];if(!cell)return out;\n    var nodes=cell.querySelectorAll('*');\n    for(var i=0;i<nodes.length;i++){\n        var el=nodes[i];if(!bwAutoIsPortElement(el))continue;\n        var side=bwAutoPortSide(el,cell);if(!side)continue;\n        out.push({element:el,side:side,index:out.length});\n    }\n    return out;\n}\n\nfunction bwAutoLogicalPorts(block){\n    var wp=worldPorts(block),ports=[];\n    (wp.inputs||[]).forEach(function(p){ports.push({id:p.id,side:p.side,kind:'input'});});\n    (wp.outputs||[]).forEach(function(p){ports.push({id:p.id,side:p.side,kind:'output'});});\n    return ports;\n}\n\nfunction bwAutoGetPortElements(block){\n    var cell=bwAutoCellElement(block.x,block.y),dom=bwAutoDomPorts(cell),logical=bwAutoLogicalPorts(block),out=[],used=[];\n    for(var i=0;i<logical.length;i++){\n        var lp=logical[i],match=null,mi=-1;\n        for(var j=0;j<dom.length;j++){\n            if(used[j])continue;\n            if(dom[j].side===lp.side){match=dom[j].element;mi=j;break;}\n        }\n        if(mi>=0)used[mi]=true;\n        out.push({id:lp.id,side:lp.side,kind:lp.kind,element:match});\n    }\n    return out;\n}\n\nfunction bwAutoFindIndex(x,y,layer){for(var i=0;i<bwAuto.bs.length;i++){var e=bwAuto.bs[i];if(Number(e.layer)===Number(layer)&&Number(e.x)===Number(x)&&Number(e.y)===Number(y))return i;}return-1;}\n\nfunction bwAutoMakeEntry(block){return{x:Number(block.x),y:Number(block.y),layer:Number(block.layer),portIndex:0,ports:bwAutoLogicalPorts(block)};}\n\nfunction bwAutoRefreshEntry(entry){\n    var block=getBlock(entry.x,entry.y,entry.layer);if(!block)return false;\n    var old=entry.ports&&entry.ports[entry.portIndex]?entry.ports[entry.portIndex].id:null;\n    entry.ports=bwAutoLogicalPorts(block);\n    if(!entry.ports.length){entry.portIndex=0;return false;}\n    if(old!==null){for(var i=0;i<entry.ports.length;i++)if(entry.ports[i].id===old){entry.portIndex=i;return true;}}\n    entry.portIndex=Math.max(0,Math.min(entry.portIndex,entry.ports.length-1));\n    return true;\n}\n\nfunction bwAutoSelectedPort(entry){if(!entry||!bwAutoRefreshEntry(entry))return null;return entry.ports[entry.portIndex]||null;}\n\nfunction bwAutoClearVisuals(){\n    document.querySelectorAll('.bw-auto-bs,.bw-auto-io-selected,[data-bw-bs=\"true\"]').forEach(function(el){el.classList.remove('bw-auto-bs','bw-auto-io-selected');el.removeAttribute('data-bw-bs');});\n}\n\nfunction bwAutoPaint(){\n    document.querySelectorAll('.bw-auto-io-selected').forEach(function(el){el.classList.remove('bw-auto-io-selected');});\n    document.querySelectorAll('.cell.bw-auto-bs,[data-bw-bs=\"true\"]').forEach(function(el){el.classList.remove('bw-auto-bs');el.removeAttribute('data-bw-bs');});\n    if(!bwAuto.active)return;\n    var kept=[];\n    for(var i=0;i<bwAuto.bs.length;i++){\n        var e=bwAuto.bs[i],block=getBlock(e.x,e.y,e.layer);if(!block||!bwAutoRefreshEntry(e))continue;\n        kept.push(e);\n        var cell=bwAutoCellElement(e.x,e.y);if(cell){cell.classList.add('bw-auto-bs');cell.setAttribute('data-bw-bs','true');}\n    }\n    bwAuto.bs=kept;\n    if(!bwAuto.bs.length){bwAuto.active=false;bwAuto.activeIndex=-1;bwAutoInvalidatePlan();return;}\n    if(bwAuto.activeIndex<0)bwAuto.activeIndex=0;\n    if(bwAuto.activeIndex>=bwAuto.bs.length)bwAuto.activeIndex=bwAuto.bs.length-1;\n    var active=bwAuto.bs[bwAuto.activeIndex],ports=bwAutoGetPortElements(getBlock(active.x,active.y,active.layer)),port=ports[active.portIndex];\n    if(port&&port.element)port.element.classList.add('bw-auto-io-selected');\n}\n\nfunction bwAutoStatus(text){if(typeof statusElement!=='undefined'&&statusElement)statusElement.textContent=text;}\n\nfunction bwAutoStatusSelection(){\n    if(!bwAuto.active)return;\n    var active=bwAuto.bs[bwAuto.activeIndex],block=active&&getBlock(active.x,active.y,active.layer),port=active&&bwAutoSelectedPort(active);if(!active||!block||!port)return;\n    var text='AUTO CONNECTION: I/O '+String(active.portIndex+1)+' ['+String(port.side||'').toUpperCase()+']';\n    if(bwAuto.bs.length<2)text+=' | ALT+CLICK SECOND BLOCK';\n    else if(bwAuto.plan&&bwAuto.plan.status==='ready')text+=' | ROUTE READY | ALT+ENTER CONNECT';\n    else if(bwAuto.plan&&bwAuto.plan.status==='failed')text+=' | NO SAFE ROUTE | CHANGE I/O OR BLOCKS';\n    else text+=' | FINDING CLOSEST ROUTE...';\n    text+=' | A/D or ←/→ I/O | ENTER CANCEL';\n    bwAutoStatus(text);\n}\n\nfunction bwAutoCancelPlanner(){\n    bwAuto.planToken++;\n    if(bwAuto.planner){try{bwAuto.planner.terminate()}catch(e){}bwAuto.planner=null;}\n    bwAuto.plan=null;\n}\n\nfunction bwAutoInvalidatePlan(){bwAutoCancelPlanner();}\n\nfunction bwAutoWorldStamp(){\n    var h=2166136261,w=Number(world.meta.grid[0])||20,hgt=Number(world.meta.grid[1])||20;\n    function mix(v){h^=String(v).charCodeAt(0)||0;h=Math.imul(h,16777619);}\n    mix(world.layers.length);mix(w);mix(hgt);\n    for(var li=0;li<world.layers.length;li++){\n        var rows=world.layers[li].schematic||[];\n        for(var y=0;y<hgt;y++){\n            var row=rows[y]||{},keys=Object.keys(row).sort(function(a,b){return Number(a)-Number(b)});\n            for(var k=0;k<keys.length;k++){\n                var x=Number(keys[k]),b=row[keys[k]];if(!b)continue;\n                mix(li);mix(x);mix(y);mix(b.type);mix(b.rotation||0);\n            }\n        }\n    }\n    return String(h>>>0);\n}\n\nfunction bwAutoCollectOccupancy(){\n    var w=Number(world.meta.grid[0])||20,h=Number(world.meta.grid[1])||20,out=[];\n    for(var li=0;li<world.layers.length;li++){\n        var rows=world.layers[li].schematic||[];\n        for(var y=0;y<h;y++){\n            var row=rows[y]||{};\n            for(var k in row){if(!Object.prototype.hasOwnProperty.call(row,k))continue;if(row[k])out.push(bwAutoPack(Number(k),y,li,w,h));}\n        }\n    }\n    return out;\n}\n\nfunction bwAutoPayload(){\n    if(bwAuto.bs.length!==2)return null;\n    var a=bwAuto.bs[0],b=bwAuto.bs[1],pa=bwAutoSelectedPort(a),pb=bwAutoSelectedPort(b),ba=getBlock(a.x,a.y,a.layer),bb=getBlock(b.x,b.y,b.layer);if(!ba||!bb||!pa||!pb)return null;\n    var start=bwAutoStep(ba.x,ba.y,pa.side),end=bwAutoStep(bb.x,bb.y,pb.side);if(!start||!end)return null;\n    return{\n        width:Number(world.meta.grid[0])||20,\n        height:Number(world.meta.grid[1])||20,\n        existingLayers:world.layers.length,\n        maxLayers:Math.min(world.layers.length+8,24),\n        occupancy:bwAutoCollectOccupancy(),\n        source:{x:Number(ba.x),y:Number(ba.y),layer:Number(ba.layer)},\n        target:{x:Number(bb.x),y:Number(bb.y),layer:Number(bb.layer)},\n        start:{x:start.x,y:start.y,layer:Number(ba.layer)},\n        end:{x:end.x,y:end.y,layer:Number(bb.layer)},\n        startDir:pa.side,\n        endDir:opposite[pb.side]\n    };\n}\n\nfunction bwAutoWorkerSource(){\n    return \"self.onmessage=function(e){var d=e.data||{},w=d.width,h=d.height,existing=Number(d.existingLayers)||1,maxLayers=Number(d.maxLayers)||existing,dirs=['top','right','bottom','left'],delta={top:[0,-1],right:[1,0],bottom:[0,1],left:[-1,0]},opp={top:'bottom',right:'left',bottom:'top',left:'right'},occ=new Set((d.occupancy||[]).map(String)),source=d.source,target=d.target,start=d.start,end=d.end,startDir=d.startDir,endDir=d.endDir;function pack(x,y,l){return Number(l)*w*h+Number(y)*w+Number(x)}function inside(x,y,l){return x>=0&&y>=0&&x<w&&y<h&&l>=0&&l<maxLayers}function stateKey(x,y,l,dir){return String(l)+':'+String(x)+':'+String(y)+':'+String(dir)}function heuristic(x,y,l){return Math.abs(x-end.x)+Math.abs(y-end.y)+Math.abs(l-end.layer)*2.6}function heapPush(a,it){var i=a.length;a.push(it);while(i>0){var p=(i-1)>>1;if(a[p].f<=it.f)break;a[i]=a[p];i=p}a[i]=it}function heapPop(a){if(!a.length)return null;var r=a[0],last=a.pop();if(a.length){var i=0;while(true){var l=i*2+1,rr=l+1;if(l>=a.length)break;var c=rr<a.length&&a[rr].f<a[l].f?rr:l;if(a[c].f>=last.f)break;a[i]=a[c];i=c}a[i]=last}return r}function occupied(x,y,l){return occ.has(String(pack(x,y,l)))}function routeDirect(){return start.x===target.x&&start.y===target.y&&start.layer===target.layer&&end.x===source.x&&end.y===source.y&&end.layer===source.layer}if(!inside(start.x,start.y,start.layer)||!inside(end.x,end.y,end.layer)){postMessage({kind:'result',route:null});return}if(routeDirect()){postMessage({kind:'result',route:{direct:true,states:[{x:start.x,y:start.y,layer:start.layer,dir:startDir},{x:end.x,y:end.y,layer:end.layer,dir:endDir}],cost:1}});return}if(occupied(start.x,start.y,start.layer)||occupied(end.x,end.y,end.layer)){postMessage({kind:'result',route:null});return}var heap=[],came=new Map(),g=new Map(),sk=stateKey(start.x,start.y,start.layer,startDir),goal=null,expanded=0,maxExpanded=Math.max(1,w*h*maxLayers*4),lastProgress=0;g.set(sk,0);heapPush(heap,{f:heuristic(start.x,start.y,start.layer),g:0,s:{x:start.x,y:start.y,layer:start.layer,dir:startDir}});while(heap.length){var cur=heapPop(heap),s=cur.s,k=stateKey(s.x,s.y,s.layer,s.dir),known=g.get(k);if(known===undefined||cur.g!==known)continue;expanded++;if(expanded-lastProgress>=500){lastProgress=expanded;postMessage({kind:'progress',value:Math.min(92,5+expanded/maxExpanded*87)});}if(s.x===end.x&&s.y===end.y&&s.layer===end.layer&&s.dir===endDir){goal=k;break}var dd=delta[s.dir],nx=s.x+dd[0],ny=s.y+dd[1];if(inside(nx,ny,s.layer)&&!occupied(nx,ny,s.layer)){var nk=stateKey(nx,ny,s.layer,s.dir),ng=cur.g+1;if(ng<(g.get(nk)===undefined?Infinity:g.get(nk))){g.set(nk,ng);came.set(nk,k);heapPush(heap,{f:ng+heuristic(nx,ny,s.layer),g:ng,s:{x:nx,y:ny,layer:s.layer,dir:s.dir}})}}for(var z=0;z<2;z++){var nl=s.layer+(z?1:-1);if(!inside(s.x,s.y,nl)||occupied(s.x,s.y,nl))continue;for(var di=0;di<4;di++){var nd=dirs[di],newLayerPenalty=nl>=existing?0.75:0,turnPenalty=nd===s.dir?0:0.2,vg=cur.g+2.6+newLayerPenalty+turnPenalty,vk=stateKey(s.x,s.y,nl,nd);if(vg<(g.get(vk)===undefined?Infinity:g.get(vk))){g.set(vk,vg);came.set(vk,k);heapPush(heap,{f:vg+heuristic(s.x,s.y,nl),g:vg,s:{x:s.x,y:s.y,layer:nl,dir:nd}})}}}}if(!goal){postMessage({kind:'progress',value:96});postMessage({kind:'result',route:null});return}var states=[],c=goal;while(c){var p=c.split(':');states.push({x:Number(p[1]),y:Number(p[2]),layer:Number(p[0]),dir:p[3]});if(c===sk)break;c=came.get(c)}states.reverse();postMessage({kind:'progress',value:96});postMessage({kind:'result',route:{direct:false,states:states,cost:g.get(goal)||states.length}})}\";\n}\n\nfunction bwAutoSearchFallback(payload,onProgress){\n    return new Promise(function(resolve){\n        var w=payload.width,h=payload.height,existing=payload.existingLayers,maxLayers=payload.maxLayers,dirs=['top','right','bottom','left'],deltas={top:[0,-1],right:[1,0],bottom:[0,1],left:[-1,0]},occ=new Set(payload.occupancy.map(String)),start=payload.start,end=payload.end,startDir=payload.startDir,endDir=payload.endDir,source=payload.source,target=payload.target;\n        function pack(x,y,l){return Number(l)*w*h+Number(y)*w+Number(x)}\n        function inside(x,y,l){return x>=0&&y>=0&&x<w&&y<h&&l>=0&&l<maxLayers}\n        function key(x,y,l,dir){return String(l)+':'+String(x)+':'+String(y)+':'+String(dir)}\n        function heuristic(x,y,l){return Math.abs(x-end.x)+Math.abs(y-end.y)+Math.abs(l-end.layer)*2.6}\n        function hp(a,it){var i=a.length;a.push(it);while(i){var p=(i-1)>>1;if(a[p].f<=it.f)break;a[i]=a[p];i=p}a[i]=it}\n        function hpop(a){if(!a.length)return null;var r=a[0],last=a.pop();if(a.length){var i=0;while(1){var l=i*2+1,rr=l+1;if(l>=a.length)break;var c=rr<a.length&&a[rr].f<a[l].f?rr:l;if(a[c].f>=last.f)break;a[i]=a[c];i=c}a[i]=last}return r}\n        function occupied(x,y,l){return occ.has(String(pack(x,y,l)))}\n        if(start.x===target.x&&start.y===target.y&&start.layer===target.layer&&end.x===source.x&&end.y===source.y&&end.layer===source.layer){resolve({direct:true,states:[{x:start.x,y:start.y,layer:start.layer,dir:startDir},{x:end.x,y:end.y,layer:end.layer,dir:endDir}],cost:1});return}\n        if(!inside(start.x,start.y,start.layer)||!inside(end.x,end.y,end.layer)||occupied(start.x,start.y,start.layer)||occupied(end.x,end.y,end.layer)){resolve(null);return}\n        var heap=[],came=new Map(),g=new Map(),sk=key(start.x,start.y,start.layer,startDir),goal=null,expanded=0,maxExpanded=Math.max(1,w*h*maxLayers*4);g.set(sk,0);hp(heap,{f:heuristic(start.x,start.y,start.layer),g:0,s:{x:start.x,y:start.y,layer:start.layer,dir:startDir}});\n        function tick(){\n            var steps=0;\n            while(heap.length&&steps<700){\n                steps++;\n                var cur=hpop(heap),s=cur.s,k=key(s.x,s.y,s.layer,s.dir),known=g.get(k);if(known===undefined||cur.g!==known)continue;\n                expanded++;if(expanded%500===0&&onProgress)onProgress(Math.min(92,5+expanded/maxExpanded*87));\n                if(s.x===end.x&&s.y===end.y&&s.layer===end.layer&&s.dir===endDir){goal=k;break}\n                var dd=deltas[s.dir],nx=s.x+dd[0],ny=s.y+dd[1];\n                if(inside(nx,ny,s.layer)&&!occupied(nx,ny,s.layer)){var nk=key(nx,ny,s.layer,s.dir),ng=cur.g+1;if(ng<(g.get(nk)===undefined?Infinity:g.get(nk))){g.set(nk,ng);came.set(nk,k);hp(heap,{f:ng+heuristic(nx,ny,s.layer),g:ng,s:{x:nx,y:ny,layer:s.layer,dir:s.dir}})}}\n                for(var z=0;z<2;z++){var nl=s.layer+(z?1:-1);if(!inside(s.x,s.y,nl)||occupied(s.x,s.y,nl))continue;for(var di=0;di<4;di++){var nd=dirs[di],vg=cur.g+2.6+(nl>=existing?0.75:0)+(nd===s.dir?0:0.2),vk=key(s.x,s.y,nl,nd);if(vg<(g.get(vk)===undefined?Infinity:g.get(vk))){g.set(vk,vg);came.set(vk,k);hp(heap,{f:vg+heuristic(s.x,s.y,nl),g:vg,s:{x:s.x,y:s.y,layer:nl,dir:nd}})}}}\n            }\n            if(goal||!heap.length){if(!goal){if(onProgress)onProgress(96);resolve(null);return}var states=[],c=goal;while(c){var p=c.split(':');states.push({x:Number(p[1]),y:Number(p[2]),layer:Number(p[0]),dir:p[3]});if(c===sk)break;c=came.get(c)}states.reverse();if(onProgress)onProgress(96);resolve({direct:false,states:states,cost:g.get(goal)||states.length});return;}\n            setTimeout(tick,0);\n        }\n        tick();\n    });\n}\n\nfunction bwAutoComputePlan(payload,token,showProgress){\n    var promise=new Promise(function(resolve,reject){\n        var finished=false,worker=null;\n        function done(route){if(finished)return;finished=true;if(worker){try{worker.terminate()}catch(e){}}if(bwAuto.planner===worker)bwAuto.planner=null;resolve(route);}\n        function fail(err){if(finished)return;finished=true;if(worker){try{worker.terminate()}catch(e){}}if(bwAuto.planner===worker)bwAuto.planner=null;reject(err);}\n        if(window.Worker&&window.Blob&&window.URL&&URL.createObjectURL){\n            try{\n                worker=new Worker(URL.createObjectURL(new Blob([bwAutoWorkerSource()],{type:'application/javascript'})));\n                bwAuto.planner=worker;\n                var url=worker.__bwUrl;\n                worker.onmessage=function(e){var m=e.data||{};if(token!==bwAuto.planToken){try{worker.terminate()}catch(ex){}return;}if(m.kind==='progress'){if(showProgress)showProgress(m.value);return;}if(m.kind==='result'){if(showProgress)showProgress(98);done(m.route||null);}};\n                worker.onerror=function(){try{worker.terminate()}catch(e){}worker=null;bwAutoSearchFallback(payload,showProgress).then(done).catch(fail)};\n                worker.postMessage(payload);\n                return;\n            }catch(e){worker=null;}\n        }\n        bwAutoSearchFallback(payload,showProgress).then(done).catch(fail);\n    });\n    return promise;\n}\n\nfunction bwAutoStartPlan(){\n    bwAutoInvalidatePlan();\n    if(!bwAuto.active||bwAuto.bs.length!==2)return null;\n    var payload=bwAutoPayload();if(!payload)return null;\n    var token=bwAuto.planToken,stamp=bwAutoWorldStamp();\n    bwAuto.plan={status:'planning',token:token,stamp:stamp,payload:payload,route:null,promise:null};\n    bwAutoStatusSelection();\n    var promise=bwAutoComputePlan(payload,token,function(v){if(bwAuto.plan&&bwAuto.plan.token===token){bwAuto.plan.progress=v;bwAutoStatusSelection();if(bwAuto.progressVisible&&window.BinaryWorldProgress)BinaryWorldProgress.update(v,'Searching closest safe route')}});\n    bwAuto.plan.promise=promise;\n    promise.then(function(route){\n        if(!bwAuto.plan||bwAuto.plan.token!==token)return;\n        bwAuto.plan.route=route;bwAuto.plan.status=route?'ready':'failed';bwAutoStatusSelection();\n    }).catch(function(){\n        if(!bwAuto.plan||bwAuto.plan.token!==token)return;\n        bwAuto.plan.route=null;bwAuto.plan.status='failed';bwAutoStatusSelection();\n    });\n    return promise;\n}\n\nfunction bwAutoSelectBlock(x,y,layer){\n    var block=getBlock(x,y,layer);if(!block)return false;\n    var index=bwAutoFindIndex(x,y,layer);\n    if(index>=0){bwAuto.active=true;bwAuto.activeIndex=index;bwAutoPaint();bwAutoStatusSelection();if(bwAuto.bs.length===2)bwAutoStartPlan();return true;}\n    var entry=bwAutoMakeEntry(block);if(!entry.ports.length){bwAutoStatus('AUTO CONNECTION: BLOCK HAS NO I/O');return false;}\n    if(bwAuto.bs.length===2)bwAuto.bs.shift();\n    bwAuto.bs.push(entry);bwAuto.active=true;bwAuto.activeIndex=bwAuto.bs.length-1;bwAutoPaint();bwAutoStatusSelection();\n    if(bwAuto.bs.length===2)setTimeout(bwAutoStartPlan,0);\n    return true;\n}\n\nfunction bwAutoCyclePort(step){\n    if(!bwAuto.active||bwAuto.activeIndex<0)return;\n    var entry=bwAuto.bs[bwAuto.activeIndex];if(!entry||!bwAutoRefreshEntry(entry))return;\n    var n=entry.ports.length;if(!n)return;\n    entry.portIndex=(entry.portIndex+step+n)%n;\n    bwAutoStartPlan();bwAutoPaint();bwAutoStatusSelection();\n}\n\nfunction bwAutoCancel(){\n    bwAuto.active=false;bwAuto.bs=[];bwAuto.activeIndex=-1;bwAuto.busy=false;bwAutoCancelPlanner();bwAuto.progressVisible=false;bwAutoClearVisuals();\n    if(window.BinaryWorldProgress&&document.getElementById('bwProgressModal')&&document.getElementById('bwProgressModal').classList.contains('visible'))document.getElementById('bwProgressModal').classList.remove('visible');\n}\n\nfunction bwAutoValidateRoute(route,from,fromPort,to,toPort){\n    if(!route)return false;if(route.direct)return true;\n    var states=route.states;if(!states||!states.length)return false;\n    var stateSet=new Set(),w=Number(world.meta.grid[0])||20,h=Number(world.meta.grid[1])||20;\n    for(var i=0;i<states.length;i++)stateSet.add(bwAutoGridKey(states[i].x,states[i].y,states[i].layer));\n    function sameState(i,j){if(i<0||j<0||i>=states.length||j>=states.length)return false;return states[i].layer===states[j].layer&&Math.abs(states[i].x-states[j].x)+Math.abs(states[i].y-states[j].y)===1;}\n    for(i=0;i<states.length;i++){\n        var s=states[i],prev=i>0?states[i-1]:null,next=i<states.length-1?states[i+1]:null;\n        if(getBlock(s.x,s.y,s.layer)&&!isSameBlock(getBlock(s.x,s.y,s.layer),from)&&!isSameBlock(getBlock(s.x,s.y,s.layer),to))return false;\n        var back=sameState(i,i-1)?prev:null,forward=sameState(i,i+1)?next:null;\n        if(back){var bd=delta[s.dir];if(back.x!==s.x-bd[0]||back.y!==s.y-bd[1])return false;}\n        if(forward){var fd=delta[s.dir];if(forward.x!==s.x+fd[0]||forward.y!==s.y+fd[1])return false;}\n        var expectedBack=back?bwAutoGridKey(back.x,back.y,back.layer):null,expectedForward=forward?bwAutoGridKey(forward.x,forward.y,forward.layer):null;\n        var ds=[opposite[s.dir],s.dir];\n        for(var di=0;di<ds.length;di++){\n            var side=ds[di],d=delta[side],nx=s.x+d[0],ny=s.y+d[1];if(nx<0||ny<0||nx>=w||ny>=h)continue;\n            var nb=getBlock(nx,ny,s.layer);if(!nb)continue;\n            var nk=bwAutoGridKey(nx,ny,s.layer);\n            if(nk===expectedBack||nk===expectedForward)continue;\n            if(i===0&&side===opposite[s.dir]&&isSameBlock(nb,from))continue;\n            if(i===states.length-1&&side===s.dir&&isSameBlock(nb,to))continue;\n            return false;\n        }\n    }\n    return true;\n}\n\nfunction bwAutoBuildPlacements(route,from,to){\n    if(route.direct)return[];\n    var states=route.states,placements=[];\n    for(var i=0;i<states.length;i++){\n        var s=states[i],block=getBlock(s.x,s.y,s.layer);\n        if(block&&((isSameBlock(block,from))||(isSameBlock(block,to))))continue;\n        var prev=i>0?states[i-1]:null,next=i<states.length-1?states[i+1]:null;\n        var verticalBefore=!!(prev&&prev.layer!==s.layer);\n        var verticalAfter=!!(next&&next.layer!==s.layer);\n        var isPasser=verticalBefore||verticalAfter;\n        var zDirection=null;\n        if(verticalAfter){\n            zDirection=next.layer>s.layer?'upper':'lower';\n        }\n        var rotation=0;\n        if(isPasser){\n            if(verticalBefore&&!verticalAfter&&next){\n                rotation=bwAutoRotation(next.dir);\n            }else if(prev){\n                rotation=bwAutoRotation(s.dir);\n            }else{\n                rotation=bwAutoRotation(s.dir);\n            }\n        }else{\n            rotation=bwAutoRotation(s.dir);\n        }\n        placements.push({x:s.x,y:s.y,layer:s.layer,type:isPasser?'passer':'connector',rotation:rotation,zDirection:zDirection});\n    }\n    return placements;\n}\n\nfunction bwAutoEnsureLayers(maxLayer){while(world.layers.length<=maxLayer)world.layers.push(createLayer('Layer '+world.layers.length));}\n\nfunction bwAutoProgressStart(){\n    bwAuto.progressVisible=true;\n    if(window.BinaryWorldProgress)BinaryWorldProgress.start('AUTO CONNECTING','Finding the closest route and orienting every Connector and Passer before placing anything.',0);\n}\nfunction bwAutoProgressUpdate(value,text){if(window.BinaryWorldProgress)BinaryWorldProgress.update(value,text||'Searching closest safe route');}\nfunction bwAutoProgressFinish(ok,text){if(window.BinaryWorldProgress){var fn=ok?BinaryWorldProgress.finish:BinaryWorldProgress.fail;fn.call(BinaryWorldProgress,ok?'CONNECTION COMPLETE':'CONNECTION NOT CREATED',text||'');}bwAuto.progressVisible=false;}\n\nfunction bwAutoPerform(){\n    if(!bwAuto.active||bwAuto.bs.length!==2||bwAuto.busy)return;\n    var plan=bwAuto.plan;\n    function execute(route){\n        if(!bwAuto.active||bwAuto.bs.length!==2){bwAuto.busy=false;return;}\n        if(!route){bwAuto.busy=false;bwAutoProgressFinish(false,'No safe route was found.');bwAutoStatus('AUTO CONNECTION: NO SAFE ROUTE FOUND');return;}\n        var a=bwAuto.bs[0],b=bwAuto.bs[1],pa=bwAutoSelectedPort(a),pb=bwAutoSelectedPort(b),from=getBlock(a.x,a.y,a.layer),to=getBlock(b.x,b.y,b.layer);\n        if(!from||!to||!pa||!pb){bwAuto.busy=false;bwAutoProgressFinish(false,'The selected blocks or I/O changed.');bwAutoStatus('AUTO CONNECTION: SELECTION CHANGED');return;}\n        if(bwAutoWorldStamp()!==(plan&&plan.stamp)){bwAuto.busy=false;bwAutoProgressFinish(false,'The world changed while the route was being prepared.');bwAutoStartPlan();return;}\n        if(!bwAutoValidateRoute(route,from,pa,to,pb)){bwAuto.busy=false;bwAutoProgressFinish(false,'The prepared route is no longer safe.');bwAutoStartPlan();return;}\n        var before=deepSnapshot(),placements=bwAutoBuildPlacements(route,from,to),maxLayer=-1;\n        for(var i=0;i<placements.length;i++)if(placements[i].layer>maxLayer)maxLayer=placements[i].layer;\n        if(maxLayer>=world.layers.length)bwAutoEnsureLayers(maxLayer);\n        for(i=0;i<placements.length;i++)if(getBlock(placements[i].x,placements[i].y,placements[i].layer)){bwAuto.busy=false;bwAutoProgressFinish(false,'A route cell became occupied.');bwAutoStartPlan();return;}\n        bwAutoProgressUpdate(99,'Placing '+String(placements.length)+' route blocks');\n        for(i=0;i<placements.length;i++){\n            var p=placements[i],runtime=createRuntime();if(p.type==='passer'&&p.zDirection)runtime.zBlock=p.zDirection;\n            setBlock(p.x,p.y,{type:p.type,x:p.x,y:p.y,layer:p.layer,rotation:p.rotation,pressed:false,state:0,runtime:runtime},p.layer);\n        }\n        finishWorldChange(before,{rebuildUI:true});\n        bwAutoProgressFinish(true,route.direct?'Direct adjacent connection.':String(placements.length)+' Connector/Passer blocks created.');\n        bwAuto.busy=false;\n        bwAutoCancel();\n    }\n    if(!plan){bwAutoStartPlan();plan=bwAuto.plan;}\n    bwAutoBusyStart(plan,execute);\n}\n\nfunction bwAutoBusyStart(plan,execute){\n    bwAuto.busy=true;bwAutoProgressStart();\n    if(!plan||!plan.promise){var p=bwAutoStartPlan();plan=bwAuto.plan;if(!plan||!p){execute(null);return;}}\n    if(plan.status==='ready'&&plan.route){bwAutoProgressUpdate(96,'Optimal route prepared');setTimeout(function(){execute(plan.route)},0);return;}\n    if(plan.status==='failed'){execute(null);return;}\n    plan.promise.then(function(route){if(!bwAuto.active){bwAuto.busy=false;return;}bwAutoProgressUpdate(96,'Optimal route prepared');setTimeout(function(){execute(route)},0)}).catch(function(){execute(null)});\n}\n\nfunction bwAutoPointerDown(e){\n    if(e.button!==0)return;\n    var cell=e.target&&e.target.closest?e.target.closest('.cell'):null;\n    if(e.altKey&&cell&&gridElement&&gridElement.contains(cell)){\n        var x=Number(cell.dataset.x),y=Number(cell.dataset.y),block=getBlock(x,y,activeLayer);\n        if(block){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();bwAutoSuppressClick=true;bwAutoSelectBlock(x,y,activeLayer);return;}\n    }\n    if(bwAuto.active)bwAutoCancel();\n}\n\nfunction bwAutoClick(e){\n    if(!bwAutoSuppressClick)return;\n    bwAutoSuppressClick=false;e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();\n}\n\nfunction bwAutoKeydown(e){\n    if(typeof isTextEditingTarget==='function'&&isTextEditingTarget(document.activeElement))return;\n    if(typeof isModalOpen==='function'&&isModalOpen()&&!(e.key==='Enter'&&e.altKey))return;\n    if(!bwAuto.active)return;\n    if(e.key==='Enter'){e.preventDefault();e.stopImmediatePropagation();if(e.altKey)bwAutoPerform();else bwAutoCancel();return;}\n    if(e.key==='Escape'){e.preventDefault();e.stopImmediatePropagation();bwAutoCancel();return;}\n    if(e.key==='ArrowRight'||e.key==='ArrowDown'||String(e.key).toLowerCase()==='d'){e.preventDefault();e.stopImmediatePropagation();bwAutoCyclePort(1);return;}\n    if(e.key==='ArrowLeft'||e.key==='ArrowUp'||String(e.key).toLowerCase()==='a'){e.preventDefault();e.stopImmediatePropagation();bwAutoCyclePort(-1);return;}\n}\n\nfunction bwAutoInstall(){\n    document.documentElement.insertAdjacentHTML('beforeend','<style id=\"binary-world-auto-connection-style\">.cell.bw-auto-bs,.cell[data-bw-bs=\"true\"]{outline:2px solid #06f!important;outline-offset:-2px;box-shadow:inset 0 0 0 1px #06f!important}.bw-auto-io-selected{box-shadow:0 0 0 3px #06f!important;border-color:#06f!important;outline:2px solid #06f!important;outline-offset:0}.outputPort.bw-auto-io-selected,.inputOutputPort.bw-auto-io-selected,.passerOutput.bw-auto-io-selected{background:#06f!important;border-color:#06f!important}.nodePort.bw-auto-io-selected,.inputPort.bw-auto-io-selected{background:#fff!important;border-color:#06f!important}</style>');\n    document.addEventListener('pointerdown',bwAutoPointerDown,true);\n    document.addEventListener('click',bwAutoClick,true);\n    document.addEventListener('keydown',bwAutoKeydown,true);\n    if(gridElement&&window.MutationObserver){\n        var observer=new MutationObserver(function(){\n            if(!bwAuto.active)return;\n            requestAnimationFrame(bwAutoPaint);\n        });\n        observer.observe(gridElement,{subtree:true,attributes:true,attributeFilter:['class'],childList:true});\n    }\n    bwAuto.paint=bwAutoPaint;\n    window.BinaryWorldAutoConnection=bwAuto;\n}\n\nbwAutoInstall();\n\n})();";
