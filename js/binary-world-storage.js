window.__BinaryWorldSourceParts=window.__BinaryWorldSourceParts||[];window.__BinaryWorldSourceParts[3]="function projectTimestampKey(timestamp){\n    var d=new Date(timestamp||Date.now()),pad=function(n){return String(n).padStart(2,'0')};\n    return'project_'+d.getFullYear()+'-'+pad(d.getMonth()+1)+'-'+pad(d.getDate())+'_'+pad(d.getHours())+'-'+pad(d.getMinutes())+'-'+pad(d.getSeconds())+'-'+String(d.getMilliseconds()).padStart(3,'0')+'_'+Math.random().toString(36).slice(2,9)\n}\n\nvar BW_STORAGE_DB_NAME='BinaryWorldDB',BW_STORAGE_DB_VERSION=1,BW_STORAGE_STORE='projects';\nvar bwStorageDBPromise=null,bwSaveQueue=Promise.resolve();\n\nfunction createUniqueProjectKey(){\n    if(window.crypto&&typeof window.crypto.randomUUID==='function')return'project_'+window.crypto.randomUUID();\n    return projectTimestampKey();\n}\n\nfunction openBinaryWorldDB(){\n    if(bwStorageDBPromise)return bwStorageDBPromise;\n    bwStorageDBPromise=new Promise(function(resolve,reject){\n        if(!window.indexedDB){reject(new Error('IndexedDB is not available in this browser.'));return}\n        var request=window.indexedDB.open(BW_STORAGE_DB_NAME,BW_STORAGE_DB_VERSION);\n        request.onupgradeneeded=function(){\n            var db=request.result,store;\n            if(!db.objectStoreNames.contains(BW_STORAGE_STORE)){\n                store=db.createObjectStore(BW_STORAGE_STORE,{keyPath:'key'});\n                store.createIndex('updatedAt','updatedAt',{unique:false});\n                store.createIndex('fileName','fileName',{unique:false});\n            }\n        };\n        request.onsuccess=function(){\n            var db=request.result;\n            db.onversionchange=function(){db.close()};\n            resolve(db);\n        };\n        request.onerror=function(){reject(request.error||new Error('Could not open IndexedDB.'))};\n        request.onblocked=function(){reject(new Error('IndexedDB is blocked by another open Binary World database connection.'))};\n    });\n    return bwStorageDBPromise;\n}\n\nfunction idbRequest(request){\n    return new Promise(function(resolve,reject){\n        request.onsuccess=function(){resolve(request.result)};\n        request.onerror=function(){reject(request.error||new Error('IndexedDB request failed.'))};\n    });\n}\n\nasync function idbPut(record){\n    var db=await openBinaryWorldDB();\n    return new Promise(function(resolve,reject){\n        var tx=db.transaction(BW_STORAGE_STORE,'readwrite');\n        tx.oncomplete=resolve;\n        tx.onerror=function(){reject(tx.error||new Error('IndexedDB save failed.'))};\n        tx.onabort=function(){reject(tx.error||new Error('IndexedDB save was aborted.'))};\n        tx.objectStore(BW_STORAGE_STORE).put(record);\n    });\n}\n\nasync function idbGet(key){\n    var db=await openBinaryWorldDB(),tx=db.transaction(BW_STORAGE_STORE,'readonly');\n    return idbRequest(tx.objectStore(BW_STORAGE_STORE).get(key));\n}\n\nasync function idbGetAll(){\n    var db=await openBinaryWorldDB(),tx=db.transaction(BW_STORAGE_STORE,'readonly');\n    return idbRequest(tx.objectStore(BW_STORAGE_STORE).getAll());\n}\n\nasync function idbDelete(key){\n    var db=await openBinaryWorldDB();\n    return new Promise(function(resolve,reject){\n        var tx=db.transaction(BW_STORAGE_STORE,'readwrite');\n        tx.oncomplete=resolve;\n        tx.onerror=function(){reject(tx.error||new Error('IndexedDB delete failed.'))};\n        tx.onabort=function(){reject(tx.error||new Error('IndexedDB delete was aborted.'))};\n        tx.objectStore(BW_STORAGE_STORE).delete(key);\n    });\n}\n\nfunction safeBWFileName(name){\n    var clean=String(name||'world').replace(/[\\\\/:*?\"<>|]/g,'_').trim()||'world';\n    if(!/\\.bw$/i.test(clean))clean+='.bw';\n    return clean;\n}\n\nasync function encodeWorldToBW(restored,progress){if(window.BinaryWorldPerformance&&typeof window.BinaryWorldPerformance.encodeWorld==='function')return window.BinaryWorldPerformance.encodeWorld(restored,progress);return await new Promise(function(resolve,reject){try{var previous=deepSnapshot();replaceWorld(restored);try{resolve(BW.writeFile(BW.buildData()))}finally{replaceWorld(normalizeJSONWorld(previous))}}catch(e){reject(e)}})}\n\nfunction recordFromWorld(key,createdAt,updatedAt,active,worldData,binary){\n    return{\n        key:key,\n        fileName:safeBWFileName(worldData.meta&&worldData.meta.name),\n        format:'BW',\n        version:BW.VERSION,\n        createdAt:Number(createdAt)||Number(updatedAt)||Date.now(),\n        updatedAt:Number(updatedAt)||Date.now(),\n        activeLayer:Number(active)||0,\n        size:binary.byteLength,\n        data:binary.buffer.slice(binary.byteOffset,binary.byteOffset+binary.byteLength)\n    };\n}\n\nfunction legacyLocalProjectRecords(){\n    var out=[];\n    for(var i=0;i<localStorage.length;i++){\n        var keyName=localStorage.key(i);\n        if(!keyName||keyName.indexOf('project_')!==0)continue;\n        var raw=null,record=null;\n        try{raw=localStorage.getItem(keyName);record=JSON.parse(raw)}catch(e){}\n        out.push({key:keyName,record:record,raw:raw});\n    }\n    return out;\n}\n\nfunction setMigrationModal(title,subtitle,index,total,name){\n    var modal=document.getElementById('storageMigrationModal'),titleEl=document.getElementById('storageMigrationTitle'),subtitleEl=document.getElementById('storageMigrationSubtitle'),fill=document.getElementById('storageMigrationFill'),count=document.getElementById('storageMigrationCount'),current=document.getElementById('storageMigrationCurrent');\n    if(!modal)return;\n    titleEl.textContent=title;\n    subtitleEl.textContent=subtitle;\n    var percent=total?Math.round((index/total)*100):0;\n    fill.style.width=percent+'%';\n    count.textContent=total?String(index)+' / '+String(total):'';\n    current.textContent=name?String(name):'';\n    modal.classList.add('visible');\n}\n\nfunction closeMigrationModal(){\n    var modal=document.getElementById('storageMigrationModal');\n    if(modal)modal.classList.remove('visible');\n}\n\nasync function migrateLegacyLocalStorage(){\n    var legacy=legacyLocalProjectRecords();\n    if(!legacy.length){\n        return{migrated:0,failed:0,total:0};\n    }\n    var migrated=0,failed=0;\n    setMigrationModal('MOVING OLD PROJECTS','Moving previous Binary World saves into IndexedDB as .bw files.',0,legacy.length,'');\n    for(var i=0;i<legacy.length;i++){\n        var item=legacy[i],record=item.record,name='Unknown project';\n        try{\n            if(record&&record.world&&record.world.meta)name=String(record.world.meta.name||'Unknown project');\n            setMigrationModal('MOVING OLD PROJECTS','Converting '+name+' to the Binary World .bw format.',i,legacy.length,name);\n            if(!record||record.format!=='BinaryWorldProject'||!record.world)throw new Error('Invalid legacy Binary World project');\n            var restored=normalizeJSONWorld(record.world),binary=await encodeWorldToBW(restored,function(p){setMigrationModal('MOVING OLD PROJECTS','Converting '+name+' to the Binary World .bw format.',i+(p/100),legacy.length,name)}),createdAt=Number(record.createdAt||record.updatedAt||Date.now()),updatedAt=Number(record.updatedAt||record.createdAt||Date.now()),active=Number(record.activeLayer)||0;\n            await idbPut(recordFromWorld(item.key,createdAt,updatedAt,active,restored,binary));\n            localStorage.removeItem(item.key);\n            migrated++;\n        }catch(error){\n            failed++;\n            try{localStorage.removeItem(item.key)}catch(e){}\n            console.error('Legacy Binary World migration failed:',item.key,error);\n        }\n        setMigrationModal('MOVING OLD PROJECTS','Completed '+name+'.',i+1,legacy.length,name);\n        await new Promise(function(resolve){setTimeout(resolve,0)});\n    }\n    setMigrationModal('MIGRATION COMPLETE','Moved '+migrated+' of '+legacy.length+' projects into IndexedDB as .bw files.',legacy.length,legacy.length,'');\n    await new Promise(function(resolve){setTimeout(resolve,450)});\n    closeMigrationModal();\n    if(failed){\n        console.warn('Binary World migration finished with '+failed+' failed legacy record(s).');\n    }\n    return{migrated:migrated,failed:failed,total:legacy.length};\n}\n\nasync function initializeBinaryWorldStorage(){\n    var legacy=legacyLocalProjectRecords();\n    if(legacy.length)setMigrationModal('MOVING OLD PROJECTS','Preparing to move previous Binary World saves into IndexedDB as .bw files.',0,legacy.length,'');\n    await openBinaryWorldDB();\n    return migrateLegacyLocalStorage();\n}\n\nwindow.BinaryWorldStorageReady=Promise.resolve().then(initializeBinaryWorldStorage);\n\nvar bwSaveTimer=null,bwSaveRevision=0,bwSaveWaiters=[];\nfunction saveCurrentProject(options){if(!currentProjectKey)return Promise.resolve();var immediate=options&&options.immediate===true;clearTimeout(bwSaveTimer);var promise=new Promise(function(resolve,reject){bwSaveWaiters.push({resolve:resolve,reject:reject})});bwSaveTimer=setTimeout(function(){performCurrentProjectSave()},immediate?0:280);return promise}\nasync function performCurrentProjectSave(){bwSaveTimer=null;if(!currentProjectKey){var none=bwSaveWaiters.splice(0);none.forEach(function(w){w.resolve()});return}var revision=++bwSaveRevision,keyName=currentProjectKey,createdAt=currentProjectCreatedAt||Date.now(),active=activeLayer,worldData=serializableWorld();try{var binary=await encodeWorldToBW(worldData);if(revision!==bwSaveRevision)return;var record=recordFromWorld(keyName,createdAt,Date.now(),active,worldData,binary);await idbPut(record);if(revision!==bwSaveRevision)return;var waiters=bwSaveWaiters.splice(0);waiters.forEach(function(w){w.resolve()})}catch(error){if(revision===bwSaveRevision){statusElement.textContent='SAVE FAILED';var waiters=bwSaveWaiters.splice(0);waiters.forEach(function(w){w.reject(error)})}console.error(error)}}\n\nfunction setLocalProjectURL(name){try{var url=new URL(window.location.href);url.searchParams.delete('load');url.searchParams.set('local',String(name||''));window.history.replaceState({},document.title,url.pathname+url.search+url.hash)}catch(e){}}\n\nasync function readLocalProjects(){\n    var projects=await idbGetAll();\n    projects=projects.filter(function(item){return item&&item.format==='BW'&&item.data});\n    projects.sort(function(a,b){return Number(b.updatedAt||b.createdAt||0)-Number(a.updatedAt||a.createdAt||0)});\n    return projects;\n}\n\nfunction formatProjectDate(ts){return ts?new Date(ts).toLocaleString():'Unknown date'}\n\nasync function openLocalProjects(){\n    localProjectList.innerHTML='';\n    var projects=[];\n    try{projects=await readLocalProjects()}catch(error){console.error(error);localProjectList.innerHTML='<div class=\"localEmpty\">IndexedDB could not be opened.</div>';localProjectsModal.classList.add('visible');return}\n    if(!projects.length){localProjectList.innerHTML='<div class=\"localEmpty\">No saved local projects.</div>'}\n    else projects.forEach(function(item){\n        var row=document.createElement('div');row.className='localProject';\n        var info=document.createElement('div');info.className='localProjectInfo';\n        var name=document.createElement('div');name.className='localProjectName';name.textContent=String(item.fileName||'Untitled.bw').replace(/\\.bw$/i,'');\n        var date=document.createElement('div');date.className='localProjectDate';date.textContent=formatProjectDate(item.updatedAt||item.createdAt)+(item.key===currentProjectKey?' \u2022 CURRENT':'');\n        info.appendChild(name);info.appendChild(date);\n        var actions=document.createElement('div');actions.className='localProjectActions';\n        var load=document.createElement('button');load.className='localProjectLoad';load.textContent='LOAD';load.onclick=function(){confirmLoadStoredProject(item.key)};\n        var del=document.createElement('button');del.className='localProjectLoad';del.textContent='DEL';del.onclick=function(){deleteLocalProject(item.key)};\n        actions.appendChild(load);actions.appendChild(del);row.appendChild(info);row.appendChild(actions);localProjectList.appendChild(row);\n    });\n    localProjectsModal.classList.add('visible');\n}\n\nasync function deleteLocalProject(keyName){\n    var item;\n    try{item=await idbGet(keyName)}catch(error){alert('Delete failed:\\n'+error.message);return}\n    if(!item){openLocalProjects();return}\n    var projectName=String(item.fileName||'Untitled.bw').replace(/\\.bw$/i,'');\n    var confirmed=await confirmDialog('Delete Project?','Delete \"'+projectName+'.bw\" from IndexedDB? This cannot be undone.');\n    if(!confirmed)return;\n    try{\n        await idbDelete(keyName);\n        if(keyName===currentProjectKey){currentProjectKey=null;currentProjectCreatedAt=0;closeModal(localProjectsModal);app.style.display='none';startup.style.display='flex';}\n        else openLocalProjects();\n    }catch(error){console.error(error);alert('Delete failed:\\n'+error.message)}\n}\n\nasync function confirmLoadStoredProject(keyName){\n    var item;\n    try{item=await idbGet(keyName)}catch(error){alert('Load failed:\\n'+error.message);return}\n    if(!item){openLocalProjects();return}\n    var projectName=String(item.fileName||'Untitled.bw').replace(/\\.bw$/i,'');\n    var confirmed=await confirmDialog('Load Project?','Load \"'+projectName+'.bw\"? Your current editor state will be replaced by the saved project.');\n    if(confirmed)await loadStoredProject(keyName);\n}\n\nasync function loadStoredProject(keyName){\n    try{\n        if(window.BinaryWorldProgress)BinaryWorldProgress.start('LOADING PROJECT','Reading the Binary World .bw file.',0);\n        var item=await idbGet(keyName);if(!item||!item.data)throw new Error('IndexedDB project no longer exists');\n        if(window.BinaryWorldProgress)BinaryWorldProgress.update(20,'Reading '+String(item.fileName||'world.bw'));\n        var bytes=item.data instanceof Uint8Array?item.data:new Uint8Array(item.data),data,restored;\n        if(window.BinaryWorldPerformance&&typeof window.BinaryWorldPerformance.decodeBW==='function')restored=await window.BinaryWorldPerformance.decodeBW(bytes,function(p){BinaryWorldProgress.update(20+p*.55,'Decoding .bw data')});else{data=BW.readFile(bytes);restored=BW.createWorld(data)}\n        currentProjectKey=String(item.key||keyName);currentProjectCreatedAt=Number(item.createdAt||item.updatedAt||Date.now());activeLayer=Math.min(Number(item.activeLayer)||0,Math.max(0,restored.layers.length-1));selected.x=null;selected.y=null;clearHistory();replaceWorld(restored);closeModal(localProjectsModal);closeModal(loadChoiceModal);if(window.BinaryWorldProgress)BinaryWorldProgress.update(82,'Building project view');await enterEditor();saveCurrentProject();setLocalProjectURL(restored.meta.name);if(window.BinaryWorldProgress)await BinaryWorldProgress.finish('PROJECT LOADED','The project is ready.');return true;\n    }catch(error){if(window.BinaryWorldProgress)BinaryWorldProgress.fail('LOAD FAILED',error.message);console.error(error);alert('Load failed:\\n'+error.message);return false}\n}\n\nfunction confirmDialog(title,subtitle){return new Promise(function(resolve){confirmResolver=resolve;confirmTitleElement.textContent=title;confirmSubtitleElement.textContent=subtitle;confirmModal.classList.add('visible');setTimeout(function(){confirmAccept.focus()},0)})}\nfunction finishConfirm(value){if(confirmResolver===null){confirmModal.classList.remove('visible');return}var resolve=confirmResolver;confirmResolver=null;confirmModal.classList.remove('visible');resolve(value===true)}\nfunction closeModal(modal){modal.classList.remove('visible')}\n\ndocument.getElementById('addLayer').onclick=function(){applyWorldChange(function(){var i=world.layers.length;world.layers.push(createLayer('Layer '+i));activeLayer=i;selected.x=null;selected.y=null},{rebuildUI:true})};\ndocument.getElementById('deleteLayer').onclick=function(){if(world.layers.length<=1){statusElement.textContent='CANNOT DELETE THE LAST LAYER';return}var layer=world.layers[activeLayer];confirmDialog('Delete Layer?','Delete \"'+layer.name+'\"? This can be undone.').then(function(ok){if(!ok)return;applyWorldChange(function(){world.layers.splice(activeLayer,1);if(activeLayer>=world.layers.length)activeLayer=world.layers.length-1;selected.x=null;selected.y=null},{rebuildUI:true})})};\nfunction buildLayerList(){layerList.innerHTML='';world.layers.forEach(function(layer,index){var el=document.createElement('div');el.className='layer'+(index===activeLayer?' active':'');el.textContent=layer.name;el.onclick=function(){clearTimeout(layerClickTimer);layerClickTimer=setTimeout(function(){if(renamingInput)return;activeLayer=index;selected.x=null;selected.y=null;buildWorld();simulation.rebuild();saveCurrentProject()},220)};el.ondblclick=function(e){e.preventDefault();e.stopPropagation();clearTimeout(layerClickTimer);beginRenameLayer(index,el)};layerList.appendChild(el)});}\nfunction buildWorld(done){buildLayerList();var w=Number(world.meta.grid[0])||20,h=Number(world.meta.grid[1])||20;var changed=w!==gridWidth||h!==gridHeight||gridCells.length!==w*h;if(changed)buildGrid(done);else{renderAll();if(typeof done==='function')done()}}\nfunction beginRenameLayer(index,element){if(renamingInput)finishRename();renamingLayerIndex=index;renamingProject=false;var input=document.createElement('input');input.className='layerRenameInput';input.value=world.layers[index].name;element.innerHTML='';element.appendChild(input);renamingInput=input;activeLayer=index;input.focus();input.select();input.onclick=function(e){e.stopPropagation()};input.onpointerdown=function(e){e.stopPropagation()};input.ondblclick=function(e){e.stopPropagation()};input.onblur=finishRename;input.addEventListener('change',function(){if(renamingInput===input)finishRename()})}\nfunction beginRenameProject(){\n    if(renamingInput)finishRename();\n    renamingProject=true;\n    renamingLayerIndex=null;\n\n    var input=document.createElement('input');\n    input.className='headerRenameInput';\n    input.value=String(world.meta.name||'');\n\n    headerTitle.innerHTML='';\n    headerTitle.appendChild(input);\n    renamingInput=input;\n\n    input.focus();\n    input.select();\n\n    input.onclick=function(e){e.stopPropagation()};\n    input.onpointerdown=function(e){e.stopPropagation()};\n    input.ontouchstart=function(e){e.stopPropagation()};\n    input.ondblclick=function(e){e.stopPropagation()};\n\n    input.onkeydown=function(e){\n        if(e.key==='Enter'){\n            e.preventDefault();\n            e.stopPropagation();\n            finishRename();\n            return;\n        }\n        if(e.key==='Escape'){\n            e.preventDefault();\n            e.stopPropagation();\n            cancelRename();\n            return;\n        }\n    };\n\n    input.onblur=function(){\n        if(renamingInput===input){\n            finishRename();\n        }\n    };\n}\n\nfunction finishRename(){\n    if(!renamingInput)return;\n\n    var input=renamingInput;\n    var value=String(input.value||'').trim();\n    var wasProject=renamingProject;\n    var layerIndex=renamingLayerIndex;\n\n    if(wasProject){\n        var oldProjectName=String(world.meta.name||'');\n\n        renamingInput=null;\n        renamingProject=false;\n        renamingLayerIndex=null;\n\n        if(value && value!==oldProjectName){\n            var beforeProject=deepSnapshot();\n            world.meta.name=value;\n            headerTitle.textContent=value;\n            pushHistory(beforeProject);\n            saveCurrentProject();\n            updateHistoryButtons();\n        }else{\n            headerTitle.textContent=world.meta.name;\n        }\n        return;\n    }\n\n    renamingInput=null;\n    renamingProject=false;\n    renamingLayerIndex=null;\n\n    if(layerIndex===null){\n        buildWorld();\n        return;\n    }\n\n    var oldLayer=String((world.layers[layerIndex]&&world.layers[layerIndex].name)||'');\n\n    if(value&&value!==oldLayer){\n        var beforeLayer=deepSnapshot();\n        world.layers[layerIndex].name=value;\n        finishWorldChange(beforeLayer);\n        return;\n    }\n\n    buildWorld();\n}\n\nfunction cancelRename(){\n    if(!renamingInput)return;\n\n    renamingInput=null;\n    renamingProject=false;\n    renamingLayerIndex=null;\n\n    headerTitle.textContent=world.meta.name;\n    buildWorld();\n}\n\nfunction isTextEditingTarget(el){if(!el)return false;var tag=String(el.tagName||'').toUpperCase();return tag==='INPUT'||tag==='TEXTAREA'||tag==='SELECT'||el.isContentEditable}\nfunction isModalOpen(){return !!document.querySelector('.modal.visible')}\ndocument.addEventListener('pointerdown',function(e){if(!renamingInput)return;if(e.target===renamingInput)return;finishRename()},true);\n\nheaderTitle.addEventListener('dblclick',function(e){e.preventDefault();e.stopPropagation();if(renamingInput||isTextEditingTarget(document.activeElement)||isModalOpen())return;beginRenameProject()});\n\nundoButton.onclick=undo;redoButton.onclick=redo;confirmCancel.onclick=function(){finishConfirm(false)};confirmAccept.onclick=function(){finishConfirm(true)};confirmModal.addEventListener('click',function(e){if(e.target===confirmModal)finishConfirm(false)});\ndocument.getElementById('exportButton').onclick=function(){exportModal.classList.add('visible')};document.getElementById('importButton').onclick=function(){importModal.classList.add('visible')};document.getElementById('exportClose').onclick=function(){closeModal(exportModal)};document.getElementById('importClose').onclick=function(){closeModal(importModal)};\n[exportModal,importModal,loadChoiceModal,localProjectsModal].forEach(function(m){m.addEventListener('click',function(e){if(e.target===m)closeModal(m)})});\n\ndocument.addEventListener('keydown',function(e){if(confirmModal.classList.contains('visible')){if(e.key==='Enter'||e.key==='Escape'){e.preventDefault();finishConfirm(e.key==='Enter')}return}if(isModalOpen()&&e.key==='Escape'){document.querySelectorAll('.modal.visible').forEach(function(m){m.classList.remove('visible')});return}if(renamingInput){if(e.key==='Enter'){e.preventDefault();finishRename();return}if(e.key==='Escape'){e.preventDefault();cancelRename();return}}var mod=e.ctrlKey||e.metaKey;if(mod&&!isTextEditingTarget(document.activeElement)&&!isModalOpen()&&e.key.toLowerCase()==='z'){e.preventDefault();if(e.shiftKey)redo();else undo();return}if(mod&&!isTextEditingTarget(document.activeElement)&&!isModalOpen()&&e.key.toLowerCase()==='y'){e.preventDefault();redo();return}if(isTextEditingTarget(document.activeElement)||isModalOpen()||renamingInput)return;if(e.code==='KeyR'||String(e.key).toLowerCase()==='r'){if(selected.x!==null&&selected.y!==null){e.preventDefault();rotateBlock(selected.x,selected.y);return}}if(selected.x===null||selected.y===null)return;if(e.key==='Delete'||e.key==='Backspace'){e.preventDefault();deleteBlock(selected.x,selected.y);return}});\n\ndocument.getElementById('createNewButton').onclick=createNewProject;document.getElementById('loadButton').onclick=function(){loadChoiceModal.classList.add('visible')};localProjectsAdd.onclick=function(){closeModal(localProjectsModal);createNewProject()};document.getElementById('loadChoiceClose').onclick=function(){closeModal(loadChoiceModal)};document.getElementById('localProjectsClose').onclick=function(){closeModal(localProjectsModal)};document.getElementById('loadImportButton').onclick=function(){closeModal(loadChoiceModal);importModal.classList.add('visible')};document.getElementById('loadLocalButton').onclick=function(){closeModal(loadChoiceModal);openLocalProjects()};createNewCancel.onclick=cancelCreateNew;createNewConfirm.onclick=confirmCreateNew;createNewModal.addEventListener('click',function(e){if(e.target===createNewModal)cancelCreateNew()});backButton.onclick=function(){commitProjectRenameIfNeeded();saveCurrentProject({immediate:true});app.style.display='none';startup.style.display='flex';};\n\nasync function enterEditor(){startup.style.display='none';app.style.display='flex';headerTitle.textContent=world.meta.name;buildTools();setMode('selector');await new Promise(function(resolve){buildWorld(resolve)});simulation.rebuild();updateHistoryButtons()}\nfunction openCreateNewModal(){createNameInput.value='';createGridXInput.value='20';createGridYInput.value='20';createNewModal.classList.add('visible');setTimeout(function(){createNameInput.focus()},0)}\nfunction cancelCreateNew(){createNewModal.classList.remove('visible')}\nasync function confirmCreateNew(){var name=String(createNameInput.value||'').trim(),w=parseInt(createGridXInput.value,10),h=parseInt(createGridYInput.value,10);if(!name){alert('NAME IS REQUIRED');createNameInput.focus();return}if(!Number.isFinite(w)||w<5||!Number.isFinite(h)||h<5){alert('GRID SIZE CANNOT BE LESS THAN 5 BY 5');return}currentProjectKey=createUniqueProjectKey();currentProjectCreatedAt=Date.now();world.meta.grid=[w,h];world.meta.name=name;world.layers=[createLayer('Layer 0')];activeLayer=0;selected.x=null;selected.y=null;clearHistory();await enterEditor();saveCurrentProject();setLocalProjectURL(name);cancelCreateNew()}\nfunction createNewProject(){openCreateNewModal()}\nfunction commitProjectRenameIfNeeded(){if(renamingInput&&renamingProject)finishRename()}window.addEventListener('beforeunload',function(){commitProjectRenameIfNeeded();saveCurrentProject({immediate:true})});document.addEventListener('visibilitychange',function(){if(document.visibilityState==='hidden'){commitProjectRenameIfNeeded();saveCurrentProject({immediate:true})}});\n\n\nasync function loadLocalProjectByName(name){\n    var wanted=String(name||'').trim();\n    if(!wanted)return false;\n    var projects=await readLocalProjects();\n    for(var i=0;i<projects.length;i++){\n        var item=projects[i],projectName=String(item.fileName||'Untitled.bw').replace(/\\.bw$/i,'');\n        if(projectName===wanted){\n            await loadStoredProject(item.key);\n            return true;\n        }\n    }\n    return false;\n}\nasync function saveWorldProject(worldData,options){var cleaned=normalizeJSONWorld(worldData),opts=options||{},key=String(opts.key||createUniqueProjectKey()),now=Date.now(),createdAt=Number(opts.createdAt)||now,updatedAt=Number(opts.updatedAt)||now,active=Number(opts.activeLayer)||0,binary=await encodeWorldToBW(cleaned,opts.progress);await idbPut(recordFromWorld(key,createdAt,updatedAt,active,cleaned,binary));return{key:key,world:cleaned};}\n\nwindow.BinaryWorldStorage={\n    saveWorld:saveWorldProject,\n    loadByKey:loadStoredProject,\n    list:readLocalProjects,\n    deleteByKey:idbDelete\n};\n\nwindow.BinaryWorldLocalLoader={loadByName:loadLocalProjectByName};\n";
